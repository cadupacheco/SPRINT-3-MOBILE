import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { 
    flex: 1, 
    padding: 16 
  },
  card: { 
    marginBottom: 12 
  },
  addButton: { 
    marginTop: 12,
    marginBottom: 24
  },
  centered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },
  cardActions: {
    flexDirection: 'row'
  }
});